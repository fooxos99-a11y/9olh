import type { ReactNode } from "react"

export const metadata = {
  title: "إدارة الألعاب | صولة وجولة",
  description: "لوحة إدارة ألعاب صولة وجولة",
}

export default function AdminLayout({ children }: { children: ReactNode }) {
  return children
}
